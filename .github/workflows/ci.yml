name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

# Temporarily reduced to only Ubuntu_GCC_JSC for LPT flake validation.
# Revert this file before merging.
jobs:
  Ubuntu_GCC_JSC:
    uses: ./.github/workflows/build-linux.yml
    with:
      cc: gcc
      cxx: g++
      js-engine: JavaScriptCore
