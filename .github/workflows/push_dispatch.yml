name: Dispatch pushes to master

on:
  push:
    branches:
      - master

jobs:
  dispatch:
    runs-on: windows-latest
    strategy:
      matrix:
        repo: ['chrisfromwork/BabylonReactNative']
    steps:
      - name: Repository Dispatch
        uses: peter-evans/repository-dispatch@v1
        with:
          token: ${{ secrets.REPO_ACCESS_TOKEN }}
          repository: ${{ matrix.repo }}
          event-type: babylonnative-master-commit
