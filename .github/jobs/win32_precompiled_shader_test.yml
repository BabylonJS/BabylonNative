parameters:
  - name: name
    type: string
  - name: vmImage
    type: string
  - name: platform
    type: string
    default: x64

jobs:
  - job: ${{parameters.name}}
    timeoutInMinutes: 30
    pool:
      vmImage: ${{parameters.vmImage}}
    variables:
      solutionName: "Win32_${{parameters.platform}}_PrecompiledShaderTest"

    steps:
      - template: cmake.yml
        parameters:
          vmImage: ${{parameters.vmImage}}

      - script: |
          cmake -G "Visual Studio 17 2022" -B build/${{variables.solutionName}} -A ${{parameters.platform}} -D BX_CONFIG_DEBUG=ON -D GRAPHICS_API=D3D11 -D CMAKE_UNITY_BUILD=$(UNITY_BUILD) -D BGFX_CONFIG_MAX_FRAME_BUFFERS=256 -D BABYLON_DEBUG_TRACE=ON -D BABYLON_NATIVE_PLUGIN_NATIVEENGINE_COMPILESHADERS=OFF
        displayName: "Generate ${{variables.solutionName}} solution"

      - task: MSBuild@1
        inputs:
          solution: "build/${{variables.solutionName}}/BabylonNative.sln"
          maximumCpuCount: true
          configuration: "RelWithDebInfo"
        displayName: "Build ${{variables.solutionName}}"

      - script: |
          reg add "HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\Windows Error Reporting\LocalDumps\PrecompiledShaderTest.exe"
          reg add "HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\Windows Error Reporting\LocalDumps\PrecompiledShaderTest.exe" /v DumpType /t REG_DWORD /d 2
          reg add "HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\Windows Error Reporting\LocalDumps\PrecompiledShaderTest.exe" /v DumpCount /t REG_DWORD /d 1
          reg add "HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows\Windows Error Reporting\LocalDumps\PrecompiledShaderTest.exe" /v DumpFolder /t REG_SZ /d "$(Build.ArtifactStagingDirectory)/Dumps"
        displayName: "Enable Crash Dumps"

      - script: |
          cd build\${{variables.solutionName}}\Apps\PrecompiledShaderTest\RelWithDebInfo
          PrecompiledShaderTest
        displayName: "Precompiled Shader Test"

      - task: CopyFiles@2
        inputs:
          sourceFolder: "build/${{variables.solutionName}}/Apps/PrecompiledShaderTest/RelWithDebInfo"
          contents: PrecompiledShaderTest.*
          targetFolder: "$(Build.ArtifactStagingDirectory)/Dumps"
          cleanTargetFolder: false
        displayName: "Stage test app exe/pdb for publishing"
        condition: failed()

      - task: PublishBuildArtifacts@1
        inputs:
          artifactName: "${{variables.solutionName}} - Crash Dumps"
          pathtoPublish: "$(Build.ArtifactStagingDirectory)/Dumps"
        displayName: "Publish ${{variables.solutionName}} Dumps"
        condition: failed()
