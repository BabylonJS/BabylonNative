if(BABYLON_NATIVE_POLYFILL_WINDOW)
    add_subdirectory(Window)
endif()

if(BABYLON_NATIVE_POLYFILL_CANVAS)
    if(BABYLON_NATIVE_POLYFILL_CANVAS_WGPU)
        add_subdirectory(CanvasWgpu)
    else()
        add_subdirectory(Canvas)
    endif()
endif()
