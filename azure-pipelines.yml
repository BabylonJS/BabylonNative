trigger:
- master

pr:
- master

variables:
- name: CMAKE_VERSION
  value: 3.19.6
- name: NDK_VERSION
  value: 22.1.7171670

jobs:
  - template: .github/jobs/win32.yml
    parameters:
      name: Win32_x64_D3D11
      vmImage: 'windows-latest'
      platform: x64

  - template: .github/jobs/win32.yml
    parameters:
      name: Win32_x64_D3D12
      vmImage: 'windows-latest'
      platform: x64
      graphics_api: D3D12
