trigger:
- master

pr:
- master

variables:
- name: CMAKE_VERSION
  value: 3.19.6
  
jobs:
# APPLE
  - template: .github/jobs/macos.yml
    parameters:
      name: MacOS
      vmImage: 'macOS-10.14'
      xCodeVersion: 11.3.1

  - template: .github/jobs/ios.yml
    parameters:
      name: iOS
      vmImage: 'macOS-10.14'
      xCodeVersion: 11.3.1
