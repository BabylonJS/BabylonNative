trigger:
- master

pr:
- master

variables:
- name: CMAKE_VERSION
  value: 3.19.6
- name: NDK_VERSION
  value: 22.1.7171670

jobs:
# WIN32
  - template: .github/jobs/win32.yml
    parameters:
      name: Win32_x64_JSI_D3D11
      vmImage: 'windows-2019'
      platform: x64
      napiType: jsi
